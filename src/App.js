import { AppBar, Toolbar, Typography } from "@material-ui/core";

function App() {
  return (
    <AppBar>
      <Toolbar>
        <Typography variant="h6">
          Anotações
        </Typography>
      </Toolbar>
    </AppBar>
  );
}

export default App;
